version: "3.9"

services:
  claw-evolve-sidecar:
    build:
      context: .
      dockerfile: sidecar/Dockerfile
    ports:
      - "8091:8091"
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      CLAW_EVOLVE_SIDECAR_API_KEY: ${CLAW_EVOLVE_SIDECAR_API_KEY:-}
